<analysis>**original_problem_statement**: The user wants to evolve their AI trading bot, SentCom, into a self-improving system by hardening the data pipeline, creating automation, and improving the UI.

A critical issue emerged where the user's local backend would freeze or crash during startup and under heavy load. This has made application stability the top priority. The primary goal is to architect a resilient system that can manage a high volume of concurrent processes (data streaming, AI analysis, UI polling) without deadlocking or crashing.

**PRODUCT REQUIREMENTS**:
1.  **Robust Data Pipeline**: Collect historical data for all required timeframes. (**COMPLETED**)
2.  **Autonomous Learning Loop**: Implement automation for data collection and model training. (**IN PROGRESS**)
3.  **Comprehensive UI**: Consolidate all features into an intuitive dashboard. (**IN PROGRESS**)
4.  **Startup Status Dashboard**: Ensure the UI correctly reflects the status of all backend services. (**IN PROGRESS**)
5.  **Comprehensive User Guide**: Create a detailed, visual, and downloadable guide. (**COMPLETED**)
6.  **Resource Prioritization System (Focus Mode)**: Implement a system to manage application resources. (**COMPLETED**)
7.  **Startup & Polling Optimization**: Implement a system to prevent the backend from being overwhelmed by frontend requests. (**NEW, IN PROGRESS**)

**User's preferred language**: English

**what currently exists?**
A full-stack trading platform (React, FastAPI, MongoDB) with a sophisticated backend that runs numerous services simultaneously. To combat backend freezes, several layers of optimization have been implemented:
*   A **Focus Mode** system to prioritize tasks and pause non-essential services.
*   A separate **** process to offload heavy jobs (AI training, data collection).
*   A **** to orchestrate a staged, wave-based loading sequence for the UI.
*   A **global request throttler** to limit all  and  calls to a maximum of 4 concurrent requests, preventing browser-side  errors.
*   A **** that performs real-time health checks on backend services.
*   Color-coded and clearly labeled terminal windows via an updated  script for easier debugging.

**Last working item**:
-   **Last item agent was working**: Fixing the . The user observed the backend was still crashing and recovering during startup. The agent diagnosed that the modal itself was contributing to the problem by sending 8 parallel health check requests every few seconds, overwhelming the server. The agent refactored the modal to check services **sequentially**, one by one, with a 750ms delay between each check.
-   **Status**: IN PROGRESS
-   **Agent Testing Done**: N
-   **Which testing method agent to use?**: Manual user testing. The user needs to  the latest code, restart the application using their  file, and observe if the startup is now stable.
-   **User Testing Done**: N

**All Pending/In progress Issue list**:
-   **Issue 1: Backend Instability During Startup (P0)**
    -   **Description**: The backend server is unstable and crashes when the frontend application loads. This is due to being overwhelmed by a high volume of simultaneous API requests from the many components mounting and polling for data at once. The symptom appears in the browser console as  or , and on the UI as status indicators turning red.
    -   **Attempted fixes**:
        1.   for staged loading (ineffective as components still mounted at once).
        2.  Global request throttler (reduced browser errors but still overloaded the backend).
        3.  The  was found to be part of the problem by sending too many parallel health checks.
    -   **Next debug checklist**:
        1.  User to  and test the latest fix (sequential health checks in ).
        2.  If the issue persists, the next step is to implement the  hook to prevent components that are not on screen from polling for data.
        3.  Check backend logs for the specific error causing the crash, not just the connection timeout.
    -   **Why fix this issue and what will be achieved with the fix?**: A stable startup is critical for the application's basic functionality. This will make the app usable again.
    -   **Status**: IN PROGRESS
    -   **Is recurring issue?**: Y
    -   **Should Test frontend/backend/both after fix?**: Both.

**In progress Task List**:
-   There are no other in-progress tasks. The startup stability issue is the sole focus.

**Upcoming and Future Tasks**
-   **Upcoming Tasks**:
    -   (P0) **User Verification of Startup Stability**: Guide the user to test the application after pulling the latest sequential health check fix.
    -   (P1) **Implement Lazy Loading**: Refactor components that use polling (e.g., ) to incorporate the already-created  hook. This will stop components from fetching data when they are not visible on the screen.
-   **Future Tasks:**
    -   (P2) **Implement Best Model Protection**: Add logic to only save new models if their accuracy is better than the currently active one.
    -   (P2) **Enable GPU for LightGBM**: Re-install LightGBM with GPU support.
    -   (P2) **Fix  Endpoint**: The  endpoint is known to hang the server.
    -   (P2) **Complete Backend Router Refactoring**: Activate modular routers in .
    -   (P2) **Setup-Specific AI Models**: Begin the project to train specialized models for each of the user's 77 trading setups.

**Completed work in this session**
-   **WebSocket Status Fix**: The Data indicator in the header now correctly shows connection status.
-   **Focus-Aware Polling**: Refactored  to be aware of the global .
-   **Worker Process Integration**: Ported Data Collection and Backtesting logic to run as jobs in .
-   **Job Management UI**: Created a  component for starting and monitoring jobs, including browser notifications.
-   **Startup Stability & Optimization (Iterative Fixes)**:
    -   Fixed  env var issue for the worker process.
    -   Improved terminal window titles and colors in the startup script for better debugging.
    -   Created a  for staged loading.
    -   Created a  that performs real health checks.
    -   Added a global request throttler to prevent browser connection pool exhaustion.
    -   Refactored the  to check services **sequentially** to reduce server load.

**Earlier issues found/mentioned but not fixed**
-   The root cause of the original backend deadlock (hanging the  endpoint) is still unknown, though the recurring startup crashes are a manifestation of this instability under load.

**Known issue recurrence from previous fork**
-   **Issue recurrence in previous fork**: Backend instability and crashes under load.
-   **Recurrence count**: High
-   **Status**: IN PROGRESS. This entire session has been focused on architecting and implementing solutions to resolve this. The latest attempt is the sequential health check in the .

**Code Architecture**


**Key Technical Concepts**
-   **Request Throttling**: Limiting the number of concurrent outbound network requests from the client to a fixed number (4) to prevent overwhelming the server and exhausting the browser's connection pool (). Implemented via a global  patch and an  interceptor.
-   **Sequential Health Checks**: Verifying backend services one-by-one with a delay, rather than all at once, to ensure the health-checking process itself does not cause a server crash.
-   **Staged/Lazy Loading**: A strategy to defer the initialization and data-loading of UI components until they are actually needed or until the application has stabilized. The  provides cosmetic staging, while the  hook is intended for true lazy loading.

**key DB schema**
-   **jobs**: 

**changes in tech stack**
-   None.

**All files of reference**
-   **/app/frontend/src/components/StartupModal.jsx**: The site of the most recent fix, now performs sequential health checks.
-   **/app/frontend/src/utils/requestThrottler.js**: The core logic for throttling API requests.
-   **/app/frontend/src/index.js**: Where the global  function is patched to use the throttler.
-   **/app/documents/TradeCommand_AITraining.bat**: The user's main startup script.
-   **/app/frontend/src/hooks/useVisibility.js**: Created to support lazy-loading, but not yet implemented in any components.

**Areas that need refactoring**:
-   Polling components should be updated to use the  hook to implement true lazy loading and further reduce unnecessary backend requests.
-   The 93 identified direct  calls could be migrated to use the central  (axios) instance for better consistency.

**key api endpoints**
-   : Create a new job for the worker process.
-   : Poll the status of a running job.
-   : A consolidated endpoint used by the  for health checks.

**Critical Info for New Agent**
-   The application is suffering from a critical stability issue where the backend crashes on startup due to being overwhelmed by requests from the frontend.
-   Your immediate priority is to guide the user through testing the latest fix: sequential health checks in the .
-   The user's workflow is: **1. Close all terminals. 2. . 3. Run .** They may need to clear browser  () to see the modal.
-   If the sequential check fix is not enough, the very next step is to implement lazy loading using the already-created  hook in components that poll for data.

**documents and test reports created in this job**
-   
-   
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User**: Reports  errors after restarting.
2.  **Agent**: Identifies the cause as the browser's connection pool being exhausted. Implements a global request throttler.
3.  **User**: Asks for clarification on clearing  before restarting.
4.  **Agent**: Provides instructions on how to clear .
5.  **User**: Provides screenshots showing the  progress fluctuating wildly, indicating the backend is crashing and recovering.
6.  **Agent**: Diagnoses that the 's own parallel health checks are now causing the crash loop.
7.  **Agent**: Implements the fix: making the modal's health checks sequential instead of parallel.
8.  **User**: Asks for clarification again on what to do.
9.  **Agent**: Re-iterates the restart steps (, restart with ).
10. **User**: Confirms they understand and will restart.

**Project Health Check:**
-   **Broken**: The application startup process is highly unstable. The backend crashes under the initial load from the frontend, making the application unusable. This is the top priority blocker.

**3rd Party Integrations**
-   Interactive Brokers (IB Gateway)
-   Ollama Pro
-   MongoDB Atlas
-   PyTorch (with CUDA)
-   LightGBM
-   ChromaDB
-   motor

**Testing status**
-   **Testing agent used after significant changes**: NO
-   **Troubleshoot agent used after agent stuck in loop**: NO
-   **Test files created**: None
-   **Known regressions**: The application is currently unstable on startup.

**Credentials to test flow:**
-   User provided their MongoDB connection string: . The database name is .

**What agent forgot to execute**
-   The agent created the  hook as part of the lazy loading plan but did not proceed to implement it in any of the data-polling components. This is a crucial next step if the current fix is insufficient.</analysis>
