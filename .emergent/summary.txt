<analysis>**original_problem_statement**: The user wants to evolve their AI trading bot, SentCom, into a self-improving system by hardening the data pipeline, creating automation, and improving the UI.

The user experienced a critical issue where the local backend would freeze during resource-intensive tasks like AI model training. This led to a new, high-priority requirement: to architect a system that can manage different operational modes (Live Trading, Data Collection, AI Training, Backtesting) to prevent the main application from becoming unresponsive. The goal is to prioritize the user's selected task (e.g., training) by pausing non-essential background services, and to offload heavy jobs to a separate process to prevent deadlocks and ensure the UI remains responsive.

**PRODUCT REQUIREMENTS**:
1.  **Robust Data Pipeline**: Collect historical data for all required timeframes. (**COMPLETED**)
2.  **Autonomous Learning Loop**: Implement automation for data collection and model training. (**IN PROGRESS**)
3.  **Comprehensive UI**: Consolidate all AI, learning, and data management features into an intuitive dashboard. (**IN PROGRESS**)
4.  **Startup Status Dashboard**: Ensure the UI correctly reflects the status of all backend services. (**IN PROGRESS**)
5.  **Comprehensive User Guide**: Create a detailed, visual, and downloadable guide. (**COMPLETED**)
6.  **Resource Prioritization System (Focus Mode)**: Implement a system to manage application resources, preventing deadlocks during heavy tasks. (**NEW, IN PROGRESS**)

**User's preferred language**: English

**what currently exists?**
A full-stack trading platform (React, FastAPI, MongoDB Atlas). The previous task to consolidate the Command Center header was completed.

Following a major backend freeze on the user's local machine, the agent implemented a two-phase Focus Mode system to manage resource contention:
*   **Phase 1 (UI Control Plane)**: A new  and a  UI dropdown in the header allow the user to switch the application's mode (e.g., to AI Training). This tells the backend to pause non-essential services like the trading bot scanner and IB data processing, prioritizing the selected task.
*   **Phase 2 (Worker Process)**: A separate  process was created to handle heavy, long-running jobs. A MongoDB-based job queue () is used to send tasks from the main app to the worker, isolating them and preventing the main app from freezing.
*   **Integration**: The AI Training UI () was re-wired to use this new job system. The user's local startup script () was updated to automatically start the new worker process.

The agent also debugged the System Status Bar, correcting endpoints for Ollama and the IB Pusher, but the Data (WebSocket) status is still not wired up correctly.

**Last working item**:
-   **Last item agent was working**: Fixing the System Status indicators in the  component. The agent identified that the WebSocket connection status was not being passed to the  to update the Data indicator.
-   **Status**: IN PROGRESS
-   **Agent Testing Done**: N
-   **Which testing method agent to use?**: screenshot tool. The goal is to verify that all four status indicators (API, IB, AI, Data) in the top  bar show the correct color-coded status, especially the Data indicator after the fix.
-   **User Testing Done**: N

**All Pending/In progress Issue list**:
-   **Issue 1: WebSocket status not reflected in UI (P0)**
    -   **Description**: The Data (quotes) indicator in the  status bar doesn't update because the connection state from  hook is not being passed to the .
    -   **Next debug checklist**: Modify the  component where  is initialized. Use the  function from  to update the status based on the  value returned by the hook.
    -   **Why fix this issue and what will be achieved with the fix?**: Provides the user with accurate, real-time feedback on the live data connection.
    -   **Status**: IN PROGRESS
    -   **Should Test frontend/backend/both after fix?**: Frontend
-   **Issue 2: Root cause of backend deadlock (P2)**
    -   **Description**: The user's local backend froze completely, requiring a force kill. This was traced to a deadlock, possibly related to a large IB data request.
    -   **Attempted fixes**: The new Worker Process architecture was implemented to prevent future deadlocks by isolating heavy tasks. This is a mitigation, not a root cause fix.
    -   **Why fix this issue and what will be achieved with the fix?**: While the worker architecture provides resilience, understanding the original deadlock could prevent other potential blocking issues in the main application thread.
    -   **Status**: NOT STARTED (Mitigated)
    -   **Is recurring issue?**: Y

**In progress Task List**:
-   There are no other in-progress tasks. The primary focus is on the pending issue above.

**Upcoming and Future Tasks**
-   **Upcoming Tasks**:
    -   (P0) **User Verification of Focus Mode System**: Guide the user to test the complete end-to-end training flow using the new UI, Focus Mode, and worker process on their local machine.
    -   (P1) **Integrate **: Refactor existing polling hooks like  to be aware of the new , so they automatically pause/resume.
    -   (P1) **Implement Data Collection & Backtesting Jobs**: Port the data collection and backtesting logic to run as jobs in the new worker process.
-   **Future Tasks:**
    -   (P2) **Implement Best Model Protection**: Add logic to only save new models if their accuracy is better than the currently active one.
    -   (P2) **Enable GPU for LightGBM**: Re-install LightGBM with GPU support.
    -   (P2) **Fix  Endpoint**: The  endpoint hangs the server.
    -   (P2) **Complete Backend Router Refactoring**: Activate modular routers in .
    -   (P2) **Setup-Specific AI Models**: Begin the multi-phase project to train specialized models for each of the user's 77 trading setups.

**Completed work in this session**
-   **Command Center UI Consolidation**: Merged the page title and Command/Charts tabs into a single, compact row in  and .
-   **Focus Mode & Worker System (Phase 1 & 2)**:
    -   Architected and implemented a system to prevent backend deadlocks by isolating heavy tasks.
    -   **Backend**: Created , , , and the  script.
    -   **Frontend**: Created  for global state and a  dropdown component in the header.
    -   **Integration**: Rewired  to create jobs for the worker instead of making direct API calls.
-   **Build Script Automation**: Updated  to automatically install the  dependency and launch the  in a separate window.
-   **Bug Fix**: Corrected the API endpoints and response parsing logic in  for the IB, Ollama, and Data Pusher status indicators.

**Earlier issues found/mentioned but not fixed**
-   None.

**Known issue recurrence from previous fork**
-   **Issue recurrence in previous fork**: Backend instability and crashes under the load of AI model training.
-   **Recurrence count**: High
-   **Status**: IN PROGRESS. The new Focus Mode and Worker architecture is the specific solution being implemented to resolve this for good.

**Code Architecture**


**Key Technical Concepts**
-   **Worker Process Architecture**: Decoupling long-running, resource-intensive tasks (like AI training) from the main real-time application by running them in a separate Python process (). This prevents the main app from freezing or deadlocking.
-   **Job Queue (MongoDB-based)**: Using a MongoDB collection ('jobs') as a simple, persistent message queue to communicate tasks from the main FastAPI application to the  process.
-   **Focus Mode State Management**: A system ( on backend,  on frontend) to globally switch the application's operational priority. This allows pausing non-essential services (e.g., bot scanning) to free up resources for a user-initiated task like training.

**key DB schema**
-   **jobs**:  (New collection for worker queue)

**changes in tech stack**
-   **motor**: New Python dependency added for asynchronous MongoDB access in the worker process.

**All files of reference**
-   **/app/frontend/src/hooks/useWebSocket.js**: Contains the  state needed for the UI fix.
-   **/app/frontend/src/contexts/SystemStatusContext.jsx**: Contains the status checking logic; needs to receive the WebSocket status.
-   **/app/frontend/src/App.js**: Where the  hook is likely used and can be connected to the context.
-   **/app/backend/worker.py**: The new isolated worker process.
-   **/app/frontend/src/components/UnifiedAITraining.jsx**: The training UI, now using the job system.
-   **/app/documents/TradeCommand_AITraining.bat**: The user's local startup script.

**Areas that need refactoring**:
-    is now largely redundant due to the more generic . It should be deprecated and its functionality fully merged into .

**key api endpoints**
-   : Get the current focus mode and priorities.
-   : Manually set the application's focus mode.
-   : Create a new job for the worker process.
-   : Poll the status of a running job.

**Critical Info for New Agent**
-   Your immediate task is to fix the Data connection status indicator. You need to find where  is used (likely ) and pass its  status to the  using the  function.
-   The user is now running the application locally using the  file. This script now starts the main app AND a new  process in a separate terminal.
-   The core architecture has shifted to a main app + worker model to solve backend freezes. When testing heavy tasks like training, verify that the job is created and the  terminal shows logs of it being processed.

**documents and test reports created in this job**
-   
-   
-   
-   
-   
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User**: Asks if the worker process starts automatically after the new UI feature is clicked.
2.  **Agent**: Clarifies that the worker is always running, but the training UI needs to be wired to the new job system. Agent then implements this wiring.
3.  **User**: Shares a screenshot of their local startup, showing all processes running. Notes that the worker failed.
4.  **User**: Provides the worker log, showing a .
5.  **Agent**: Fixes the  script to install  and tells the user how to fix it locally.
6.  **User**: Confirms the Focus Mode selector is visible but asks why the system status indicators are still not working correctly.
7.  **Agent**: Investigates the status indicators, finds incorrect API endpoints and response parsing, and fixes them in .
8.  **Agent**: Identifies the final missing piece: the WebSocket connection status is not being propagated to the context.
9.  **User**: States that they just did the health check and its not returning anything
10. **Agent**: Diagnoses the backend is blocked/frozen, and provides instructions to kill the process.

**Project Health Check:**
-   **Broken**:
    -   The Data (WebSocket) status indicator in the  component is not functional.
    -   The  endpoint is still known to hang the server.
-   **Mocked**: None.

**3rd Party Integrations**
-   Interactive Brokers (IB Gateway)
-   Ollama Pro
-   MongoDB Atlas
-   PyTorch (with CUDA)
-   LightGBM
-   ChromaDB
-   motor (new)

**Testing status**
-   **Testing agent used after significant changes**: NO
-   **Troubleshoot agent used after agent stuck in loop**: NO
-   **Test files created**: None
-   **Known regressions**: None.

**Credentials to test flow:**
-   User provided their MongoDB connection string in the previous fork's handoff summary: . Use the DB name .

**What agent forgot to execute**
-   The agent did not complete the fix for the system status bar. The final step of connecting the  hook's  state to the  is still pending.</analysis>
