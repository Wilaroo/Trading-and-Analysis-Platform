# TradeCommand Local ML Requirements
# ===================================
# For local development with NVIDIA RTX 5060 Ti (CUDA 12.8)
#
# INSTALLATION ORDER:
# 1. First install PyTorch with CUDA 12.8:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# 2. Then install the rest:
#    pip install -r requirements_local_ml.txt

# ===================
# ML Core Libraries
# ===================
lightgbm>=4.0.0              # Time-series AI predictions (GBM models)
scikit-learn>=1.3.0          # ML utilities, preprocessing, metrics
scipy>=1.11.0                # Scientific computing

# ===================
# Deep Learning
# ===================
# NOTE: PyTorch must be installed separately with CUDA support (see above)
# torch - DO NOT include here, install via cu128 index
transformers>=4.30.0         # NLP models, tokenizers
sentence-transformers>=2.2.0 # Text embeddings for RAG

# ===================
# Vector Database (RAG)
# ===================
chromadb>=0.4.0              # Vector store for knowledge base

# ===================
# Data Processing
# ===================
numpy>=1.24.0                # Numerical computing
pandas>=2.0.0                # Data manipulation

# ===================
# Embedding Support
# ===================
tokenizers>=0.13.0           # Fast tokenization
safetensors>=0.3.0           # Safe model weight storage

# ===================
# Optional Enhancements
# ===================
# xgboost>=2.0.0             # Alternative gradient boosting (if needed)
# onnxruntime-gpu>=1.16.0    # ONNX inference with GPU (for model export)

# ===================
# Verification Commands
# ===================
# After installation, verify with:
#   python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA: {torch.cuda.is_available()}'); print(f'GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"N/A\"}')"
#   python -c "import lightgbm; print(f'LightGBM: {lightgbm.__version__}')"
#   python -c "import chromadb; print(f'ChromaDB: {chromadb.__version__}')"
