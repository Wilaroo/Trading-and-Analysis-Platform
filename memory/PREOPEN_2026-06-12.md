# Pre-Open Checklist — 2026-06-12 (first session with v322t + v322u live)

All times ET. Everything below is read-only verification — no patches, no
code changes before the open. Commands run on the DGX from
`~/Trading-and-Analysis-Platform` unless noted.

## T-30min (~9:00) — Boot
1. Run `StartTrading.bat` as normal (step 2 git pull brings cb1c356b live).
2. Confirm the pull landed: `git log --oneline -1` → expect `cb1c356b`.
3. Health: `curl -s localhost:8001/api/health` → 200.

## T-25min — Boot-log triage (one command)
grep -E "v322s MISSED-EOD|v322u TAXONOMY|Restored .* open trades|Traceback" /tmp/backend.log
- `Restored N open trades` → N matches expectation
- `[v322s MISSED-EOD]` → silence expected; if GLD 54ad8355 carried, a
  CRITICAL alarm + flatten IS the fix working
- Zero Tracebacks

## T-20min — v322t spot-check (created_at + m0_legs survived restore)
curl -s "localhost:8001/api/trading-bot/trades/open" (verify created_at not
today's boot time, scale_out_config populated for any restored trade)

## T-15min — Plumbing
- Pusher quotes fresh (SPY/VIX snapshot age < 60s)
- IB Gateway port 4002 up; boot reconcile pill green
- Kill switch armed for new day; `bot_state.mode` correct
- stocks_in_play refreshed; scanner ticking

## 9:30-10:30 — Live watch
- `[v19.34.8 REJECTION-COOLDOWN] ... STARTED` lines on broker rejections =
  v322u working; NO same symbol/setup submitted >2x/min (re-fire churn dead)
- `[v322u TAXONOMY]` breadcrumbs = style/tf reconciler firing
- First scalp fill: M0 3-leg OCA ladder visible at IB (P0 validation)
- `[v19.34.171 SCALP-DECAY]` only ever flattens scalp-style trades

## 15:40-16:00 — EOD
- 15:45 flatten fires; `/api/trading-bot/trades/open` → [] by 15:59:30
- No new `missed_eod_carryover` rows in state_integrity_events
