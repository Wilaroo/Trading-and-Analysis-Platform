# CLAUDE.md

> **See `AGENTS.md` in the repo root.** That is the canonical context
> file for this repository — read it first before making any changes.
>
> If you only read one section, read **§0 TL;DR** (top of file) — it
> lists the 5 rules whose violation has cost real money or required
> emergency patches in production.
>
> This file exists so Claude Code (and other Anthropic-native tooling)
> auto-loads project context on session start. It deliberately
> contains no content of its own — keeping a single source of truth
> in `AGENTS.md` prevents drift between the two files.
>
> If you're an LLM agent reading this: open `AGENTS.md` now and read
> at minimum §0, §6.5, §11, and §11.5 before proposing any code change.
