# CLAUDE.md

> **See `AGENTS.md`.** That is the canonical context file for this
> repository — read it first before making any changes.
>
> This file exists so Claude Code (and other Anthropic-native tooling)
> auto-loads project context on session start. It deliberately
> contains no content of its own — keeping a single source of truth
> in `AGENTS.md` prevents drift between the two files.
>
> If you're an LLM agent reading this: open `AGENTS.md` now, read at
> minimum sections 1, 2, 3, 6, 6.5, 11, and 11.5 before proposing any
> code change.
